use std::io;

fn main() {

    println!("Please enter first long integer >>");

    let mut int1 = LongInt::new();

    let mut tmp = BufReader::new

    //Trying to replicate overloaded input operator
    let int1.num_digits = 0;
    while()

    println!("Please enter second long integer >>");

    let mut int2 = LongInt::new();
    io::stdin().read_line(&mut int2).expect("Failed to read line");

    let int3 = int1 + int2;

    println!(int3);
}
